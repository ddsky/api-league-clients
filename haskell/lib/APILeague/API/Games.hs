{-
   API League

   API League is a Hub for World Class APIs.

   OpenAPI Version: 3.0.0
   API League API version: 1.8.1
   Contact: mail@apileague.com
   Generated by OpenAPI Generator (https://openapi-generator.tech)
-}

{-|
Module : APILeague.API.Games
-}

{-# LANGUAGE FlexibleContexts #-}
{-# LANGUAGE FlexibleInstances #-}
{-# LANGUAGE MonoLocalBinds #-}
{-# LANGUAGE MultiParamTypeClasses #-}
{-# LANGUAGE OverloadedStrings #-}
{-# OPTIONS_GHC -fno-warn-name-shadowing -fno-warn-unused-binds -fno-warn-unused-imports #-}

module APILeague.API.Games where

import APILeague.Core
import APILeague.MimeTypes
import APILeague.Model as M

import qualified Data.Aeson as A
import qualified Data.ByteString as B
import qualified Data.ByteString.Lazy as BL
import qualified Data.Data as P (Typeable, TypeRep, typeOf, typeRep)
import qualified Data.Foldable as P
import qualified Data.Map as Map
import qualified Data.Maybe as P
import qualified Data.Proxy as P (Proxy(..))
import qualified Data.Set as Set
import qualified Data.String as P
import qualified Data.Text as T
import qualified Data.Text.Encoding as T
import qualified Data.Text.Lazy as TL
import qualified Data.Text.Lazy.Encoding as TL
import qualified Data.Time as TI
import qualified Network.HTTP.Client.MultipartFormData as NH
import qualified Network.HTTP.Media as ME
import qualified Network.HTTP.Types as NH
import qualified Web.FormUrlEncoded as WH
import qualified Web.HttpApiData as WH

import Data.Text (Text)
import GHC.Base ((<|>))

import Prelude ((==),(/=),($), (.),(<$>),(<*>),(>>=),Maybe(..),Bool(..),Char,Double,FilePath,Float,Int,Integer,String,fmap,undefined,mempty,maybe,pure,Monad,Applicative,Functor)
import qualified Prelude as P

-- * Operations


-- ** Games

-- *** findSimilarGamesAPI

-- | @GET \/list-similar-games@
-- 
-- Find Similar Games API
-- 
-- Find similar games based on a given game using AI. This API allows you to retrieve a list of games that are similar to a specified game, providing recommendations based on various factors such as genre, gameplay mechanics, and user preferences.
-- 
-- AuthMethod: 'AuthApiKeyApiKey', 'AuthApiKeyHeaderApiKey'
-- 
findSimilarGamesAPI
  :: Id -- ^ "id" -  The id of the game.
  -> APILeagueRequest FindSimilarGamesAPI MimeNoContent FindSimilarGamesAPI200Response MimeJSON
findSimilarGamesAPI (Id id) =
  _mkRequest "GET" ["/list-similar-games"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthApiKeyApiKey)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthApiKeyHeaderApiKey)
    `addQuery` toQuery ("id", Just id)

data FindSimilarGamesAPI  

-- | /Optional Param/ "limit" - Number of results to return between 1 and 10.
instance HasOptionalParam FindSimilarGamesAPI Limit where
  applyOptionalParam req (Limit xs) =
    req `addQuery` toQuery ("limit", Just xs)
-- | @application/json@
instance Produces FindSimilarGamesAPI MimeJSON


-- *** retrieveGameById

-- | @GET \/retrieve-game@
-- 
-- Retrieve Game by Id
-- 
-- This API allows you to retrieve detailed information about a specific game by its unique identifier. Game details include the game's name, description, release date, developer, platforms, genres, tags, ratings, screenshots, videos, and more.
-- 
-- AuthMethod: 'AuthApiKeyApiKey', 'AuthApiKeyHeaderApiKey'
-- 
retrieveGameById
  :: Id -- ^ "id" -  The id of the game.
  -> APILeagueRequest RetrieveGameById MimeNoContent RetrieveGameById200Response MimeJSON
retrieveGameById (Id id) =
  _mkRequest "GET" ["/retrieve-game"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthApiKeyApiKey)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthApiKeyHeaderApiKey)
    `addQuery` toQuery ("id", Just id)

data RetrieveGameById  
-- | @application/json@
instance Produces RetrieveGameById MimeJSON


-- *** searchGamesAPI

-- | @GET \/search-games@
-- 
-- Search Games API
-- 
-- Search through a vast database of over half a million video games from more than 50 platforms. This API allows you to find games based on various criteria such as genre, platform, release date, and more. The results include detailed information about each game, including ratings, descriptions, and images.
-- 
-- AuthMethod: 'AuthApiKeyApiKey', 'AuthApiKeyHeaderApiKey'
-- 
searchGamesAPI
  :: APILeagueRequest SearchGamesAPI MimeNoContent SearchGamesAPI200Response MimeJSON
searchGamesAPI =
  _mkRequest "GET" ["/search-games"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthApiKeyApiKey)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthApiKeyHeaderApiKey)

data SearchGamesAPI  

-- | /Optional Param/ "query" - The search query.
instance HasOptionalParam SearchGamesAPI Query where
  applyOptionalParam req (Query xs) =
    req `addQuery` toQuery ("query", Just xs)

-- | /Optional Param/ "offset" - Pagination offset (start index) between 0 and 1000.
instance HasOptionalParam SearchGamesAPI Offset where
  applyOptionalParam req (Offset xs) =
    req `addQuery` toQuery ("offset", Just xs)

-- | /Optional Param/ "limit" - Number of results to return between 1 and 100.
instance HasOptionalParam SearchGamesAPI Limit where
  applyOptionalParam req (Limit xs) =
    req `addQuery` toQuery ("limit", Just xs)

-- | /Optional Param/ "filters" - JSON array of filters
instance HasOptionalParam SearchGamesAPI Filters where
  applyOptionalParam req (Filters xs) =
    req `addQuery` toQuery ("filters", Just xs)

-- | /Optional Param/ "sort" - Field to sort by
instance HasOptionalParam SearchGamesAPI Sort where
  applyOptionalParam req (Sort xs) =
    req `addQuery` toQuery ("sort", Just xs)

-- | /Optional Param/ "sort-order" - Sort order (asc or desc)
instance HasOptionalParam SearchGamesAPI SortOrder where
  applyOptionalParam req (SortOrder xs) =
    req `addQuery` toQuery ("sort-order", Just xs)

-- | /Optional Param/ "generate-filter-options" - Whether to generate filter options
instance HasOptionalParam SearchGamesAPI GenerateFilterOptions where
  applyOptionalParam req (GenerateFilterOptions xs) =
    req `addQuery` toQuery ("generate-filter-options", Just xs)
-- | @application/json@
instance Produces SearchGamesAPI MimeJSON

