/**
 * API League
 * API League is a Hub for World Class APIs.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: mail@apileague.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ScoreText200ResponseSkimmabilitySubscores model module.
 * @module model/ScoreText200ResponseSkimmabilitySubscores
 * @version 1.0.0
 */
class ScoreText200ResponseSkimmabilitySubscores {
    /**
     * Constructs a new <code>ScoreText200ResponseSkimmabilitySubscores</code>.
     * @alias module:model/ScoreText200ResponseSkimmabilitySubscores
     */
    constructor() { 
        
        ScoreText200ResponseSkimmabilitySubscores.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ScoreText200ResponseSkimmabilitySubscores</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ScoreText200ResponseSkimmabilitySubscores} obj Optional instance to populate.
     * @return {module:model/ScoreText200ResponseSkimmabilitySubscores} The populated <code>ScoreText200ResponseSkimmabilitySubscores</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ScoreText200ResponseSkimmabilitySubscores();

            if (data.hasOwnProperty('bullet_point_ratio_score')) {
                obj['bullet_point_ratio_score'] = ApiClient.convertToType(data['bullet_point_ratio_score'], ['Number']);
            }
            if (data.hasOwnProperty('image_score')) {
                obj['image_score'] = ApiClient.convertToType(data['image_score'], ['Number']);
            }
            if (data.hasOwnProperty('highlighted_word_ratio_score')) {
                obj['highlighted_word_ratio_score'] = ApiClient.convertToType(data['highlighted_word_ratio_score'], ['Number']);
            }
            if (data.hasOwnProperty('video_score')) {
                obj['video_score'] = ApiClient.convertToType(data['video_score'], ['Number']);
            }
            if (data.hasOwnProperty('paragraph_score')) {
                obj['paragraph_score'] = ApiClient.convertToType(data['paragraph_score'], ['Number']);
            }
            if (data.hasOwnProperty('paragraph_headline_ratio_score')) {
                obj['paragraph_headline_ratio_score'] = ApiClient.convertToType(data['paragraph_headline_ratio_score'], ['Number']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ScoreText200ResponseSkimmabilitySubscores</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ScoreText200ResponseSkimmabilitySubscores</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['bullet_point_ratio_score'])) {
            throw new Error("Expected the field `bullet_point_ratio_score` to be an array in the JSON data but got " + data['bullet_point_ratio_score']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['image_score'])) {
            throw new Error("Expected the field `image_score` to be an array in the JSON data but got " + data['image_score']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['highlighted_word_ratio_score'])) {
            throw new Error("Expected the field `highlighted_word_ratio_score` to be an array in the JSON data but got " + data['highlighted_word_ratio_score']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['video_score'])) {
            throw new Error("Expected the field `video_score` to be an array in the JSON data but got " + data['video_score']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['paragraph_score'])) {
            throw new Error("Expected the field `paragraph_score` to be an array in the JSON data but got " + data['paragraph_score']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['paragraph_headline_ratio_score'])) {
            throw new Error("Expected the field `paragraph_headline_ratio_score` to be an array in the JSON data but got " + data['paragraph_headline_ratio_score']);
        }

        return true;
    }


}



/**
 * @member {Array.<Number>} bullet_point_ratio_score
 */
ScoreText200ResponseSkimmabilitySubscores.prototype['bullet_point_ratio_score'] = undefined;

/**
 * @member {Array.<Number>} image_score
 */
ScoreText200ResponseSkimmabilitySubscores.prototype['image_score'] = undefined;

/**
 * @member {Array.<Number>} highlighted_word_ratio_score
 */
ScoreText200ResponseSkimmabilitySubscores.prototype['highlighted_word_ratio_score'] = undefined;

/**
 * @member {Array.<Number>} video_score
 */
ScoreText200ResponseSkimmabilitySubscores.prototype['video_score'] = undefined;

/**
 * @member {Array.<Number>} paragraph_score
 */
ScoreText200ResponseSkimmabilitySubscores.prototype['paragraph_score'] = undefined;

/**
 * @member {Array.<Number>} paragraph_headline_ratio_score
 */
ScoreText200ResponseSkimmabilitySubscores.prototype['paragraph_headline_ratio_score'] = undefined;






export default ScoreText200ResponseSkimmabilitySubscores;

